name: balancer_arb_mobile
description: "Balancer V3 Flash Loan Arbitrage Bot for Polygon"
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  http: ^1.1.0
  provider: ^6.0.5
  shared_preferences: ^2.2.2
  web_socket_channel: ^2.4.0
  flutter_dotenv: ^5.1.0
  intl: ^0.18.1
  fl_chart: ^0.65.0
  google_fonts: ^6.1.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.1

flutter:
  uses-material-design: true
  assets:
    - .env
    - assets/